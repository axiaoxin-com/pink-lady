[server]
mode = "debug"
bind = "localhost:4869"
sentrydsn = ""

[pprof]
open = true
path = "/x/debug/pprof"

[logger]
level = "debug"
format = "json"
outputPaths = ["stderr", "/tmp/pink-lady.log"]
disableCaller = false
disableStacktrace = false
logRequestInfo = true

[database]
sqlite3 = [
    {
        instance = "default",
        dbname = "/tmp/pink-lady.db",
        maxIdleConns = 10,  # 设置连接池中的最大闲置连接数
        maxOpenConns = 10,  # 设置数据库的最大连接数量
        connMaxLifeMinutes = 60,  # 设置连接的最大可复用时间
        logMode = true
    },
    {
        instance = "another",
        dbname = "/tmp/another-pink-lady.db",
        maxIdleConns = 10,
        maxOpenConns = 10,
        connMaxLifeMinutes = 60,
        logMode = true
    }
]

mysql = [
    {
        instance = "default",
        host = "127.0.0.1",
        port = 3306,
        dbname = "test",
        username = "root",
        password = "roooooot",
        maxIdleConns = 10,  # 设置连接池中的最大闲置连接数
        maxOpenConns = 10,  # 设置数据库的最大连接数量
        connMaxLifeMinutes = 60,  # 设置连接的最大可复用时间
        timeout = 5,  # 连接超时时间（秒）
        logMode = true
    }
]

postgres = [
    {
        instance = "default",
        host = "127.0.0.1",
        port = 5432,
        dbname = "test",
        username = "root",
        password = "roooooot",
        sslmode = "disable",
        maxIdleConns = 10,
        maxOpenConns = 10,
        connMaxLifeMinutes = 60,
        logMode = true
    }
]

[redis]
mode = "single-instance"
address = "localhost:6379"
password = ""
dbindex = 0
master = ""

[apidocs]
rootRedirect = true
basicauth.username = "admin"
basicauth.password = "!admin"
